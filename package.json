{
  "name": "gh-pages-example",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tailwindcss build src/style.css -o src/style.compiled.css",
    "deploy": "npm run build && gh-pages -d src"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gh-pages": "^3.1.0",
    "tailwindcss": "^1.9.0"
  }
}
